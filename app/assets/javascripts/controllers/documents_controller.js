// App.DocumentsController = Ember.ArrayController.extend({
//   sortProperties: ['id']
// });

App.DocumentsIndexController = Ember.ArrayController.extend({
  sortProperties: ['id']
});

App.DocumentsSelectController = Ember.ArrayController.extend({
  sortProperties: ['id']
});

App.DocumentsShowController = Ember.ObjectController.extend({

});

